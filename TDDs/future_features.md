


Version 55 features - run-time stage
==================

- [] when the flowchart is RUN (not debugged)
  - [] hide the tools (Inspector panel, Data panel)
  - [] hide the flowchart
- so user just sees the 'stage'
  - [] if

- [] and add Vite and PlayWright tests for the above feature(s)



Version X features - build runtime
==================

this feature is about biulding a runtime version of the user experience, without the flowchart editor at all

- [] add a "Build" button
  - [] this creates a "flowchart-build" folder (replacing any existing contents of such a folder)
  - [] it copies the /public resources into that folder
  - [] it writes HTML/CSS/JS files to create a web page that is a runnable version of the flowchart

- [] and add Vite and PlayWright tests for the above feature(s)

Version X features 
==================

- [] AAAAA

- [] and add Vite and PlayWright tests for the above feature(s)
